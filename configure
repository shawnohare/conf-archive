#! /usr/bin/env sh
# Minimally prepare the environment.

CONF_HOME="${CONF_HOME:-${HOME}/conf}"

. "${CONF_HOME}/home/.env"
. "${CONF_HOME}/home/.profile"

make --file="${CONF_HOME}/makefile"
echo "Configuration complete. Restart the shell or source ~/.profile."
# make install
