[filter "media"]
	required = true
	clean = git media clean %f
	smudge = git media smudge %f
[push]
	default = simple
[include]
  path = ~/.gitconfig.local
[credential]
	helper = osxkeychain
[core]
	excludesfile = ~/.gitignore_global
