#!/usr/bin/env bash
# User configuration manage executable.

# TODO
# - Determine pwd if CONF_HOME not set.

# XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-${HOME}/.config}"
# CONF_HOME="${CONF_HOME:-${XDG_CONFIG_HOME}/conf}"

cmd::bootstrap() {
  # TODO
}

cmd::link() {
  "${CONF_HOME}/bin/link" $1 $$
}

cmd::mkdirs() {
  # TODO
}

cmd::unlink() {
  # TODO
}


# Install a package.
cmd::install() {
  # Could have a package structure like:
  # ~/.config/conf/pkgs/$pkg/{install,uninstall}

  # Then conf install $pkg would just map to this install executable.
  # Bootstrap to install 
}

