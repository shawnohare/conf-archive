#! /usr/bin/env bash

install_rust() {
    if [ ! -d "${CARGO_HOME}" ]; then
        curl https://sh.rustup.rs -sSf | bash -s -- --no-modify-path
    fi
    # To uninstall, just do rustup self uninstall
}

# export PKG_CONFIG_PATH=/usr/local/opt/openssl/lib/pkgconfig

pkgs=(
   # cargo-update
   # exa
   # ripgrep
)

install_rust
for pkg in "${pkgs[@]}"; do
  cargo install --force "${pkg}"
done
