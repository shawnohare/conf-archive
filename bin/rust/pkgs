#! /usr/bin/env bash
# export PKG_CONFIG_PATH=/usr/local/opt/openssl/lib/pkgconfig

pkgs=(
   # cargo-update
   # exa
   # ripgrep
)

for pkg in "${pkgs[@]}"; do
  cargo install --force "${pkg}"
done
