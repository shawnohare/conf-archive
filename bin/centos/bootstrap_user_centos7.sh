# For bootstrapping on a machine without sudo access.


echo "Installing linux brew for external apps such as tmux"
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"
