#!/usr/bin/env zsh

source "${ZPLUGIN_HOME}/plugins.zsh"

function update() {
  echo "Updating: $1"
  cd "${ZPLUGIN_HOME}/$1" && [[ -d ".git" ]] && \
    git diff-index --quiet HEAD && git pull
}

for plugin in "${plugins[@]}"; do
  update "${plugin}"
done

