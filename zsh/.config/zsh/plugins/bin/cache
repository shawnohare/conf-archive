#!/usr/bin/env zsh

source "${ZPLUGIN_HOME}/bin/plug.zsh" cache
echo "Static file located at ${ZPLUGIN_CACHE}"
rm "${ZPLUGIN_CACHE}" > /dev/null
echo "# Automatically generated by plugin manager." >> "${ZPLUGIN_CACHE}"
source "${ZPLUGIN_HOME}/plugins.zsh"
