#!/usr/bin/env zsh
# Install or update plugins listed in the plugin file. Cache the results.
#
source "${ZPLUGIN_HOME}/bin/plug.zsh" install
source "${ZPLUGIN_HOME}/plugins.zsh"
"${ZPLUGIN_HOME}/bin/cache"
